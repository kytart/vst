build/
.DS_Store
*.user

# JUCE's JS frontend library, copied into each plugin by CMake at configure
# time. Patterns containing a slash are anchored to this file's directory, so
# this needs the ** to reach into plugins/*/.
**/ui/public/js/juce/
